# Local AI Developer Toolkit

[English version](README.md)

[![Build and test](https://github.com/MrAliev/local-ai-developer-toolkit/actions/workflows/build-and-test.yml/badge.svg)](https://github.com/MrAliev/local-ai-developer-toolkit/actions/workflows/build-and-test.yml)
[![Latest release](https://img.shields.io/github/v/release/MrAliev/local-ai-developer-toolkit?display_name=tag)](https://github.com/MrAliev/local-ai-developer-toolkit/releases/latest)
[![License: MIT](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

## Обзор

Local AI Developer Toolkit — решение на .NET, которое запускает инструменты разработчика на локальных
моделях через один устойчивый брокер, общий для всей машины. Оно объединяет семантический поиск по
коду с учётом репозитория, интеграции MCP, помощники локальных задач и синхронизацию индекса с
учётом Git — не меняя исходные проекты просто потому, что репозиторий открыли.

Решение, пространства имён, команды и каталог рантайма по-прежнему называются `LocalAi`, `CodeSearch`
и `LocalLm`. Имя репозитория описывает инструментарий целиком и не переименовывает эти устоявшиеся
контракты.

## Основные возможности

- Устойчивая FIFO-очередь брокера, общая для всей машины, — единственный поддерживаемый транспорт к
  Ollama: работа Codex, Claude, CodeSearch и LocalLm выполняется строго последовательно.
- Watchdog backend отделён от heartbeat аренды. У долгих задач нет общего ограничения по времени: после десяти минут без завершения broker проверяет доступность backend и residency модели и завершает попытку только после двух подряд подтверждённых отрицательных результатов. Timeout или неопределённый probe не прерывает здоровую многочасовую операцию.
- Синхронизация репозитория атомарно сохраняет безопасный progress snapshot. `index_status` показывает фазу, обработанные/всего/оставшиеся чанки, измеренную скорость и ETA во время сборки generation, не раскрывая тексты чанков или prompt jobs.
- Маршрутизация с учётом моделей выбирает подходящую модель по возможностям, текущей резидентности в VRAM, ограничению контекста, состоянию эксперимента и установленному порядку резервных вариантов.
- Снимки с учётом модели группируют работу для одной и той же модели, пропускают вперёд более короткие
  совместимые задачи и не дают задаче голодать дольше 15 минут.
- CodeSearch сочетает векторное сходство с точным лексическим совпадением: семантический и лексический
  поиск по коду ведётся с учётом репозитория.
- Неизменяемые базовые поколения соответствуют выбранной локальной основной ветке, а точные overlay для
  каждого рабочего дерева содержат отличия ветки и незакоммиченного содержимого.
- CodeSearch и LocalLm предоставляют stdio-серверы MCP для интеграции с совместимыми AI-клиентами.
- Синхронизация репозитория и общие Git-хуки подключаются только явно, по решению владельца.
- Рекомендуемые модели обнаруживаются и устанавливаются через MCP без удаления существующих запасных моделей.

## Как компоненты работают вместе

```text
Codex / Claude / MCP clients
          |
          +--> CodeSearch MCP/CLI --> repository index + worktree overlay
          |
          +--> LocalLm MCP --------> local task helpers
                          |
                          v
                 LocalAi FIFO broker
                          |
                          v
                        Ollama
```

Брокер отвечает за транспорт к моделям и за устойчивость очереди. CodeSearch отвечает за нарезку на
чанки, эмбеддинги, неизменяемые поколения индекса, overlay и гибридный поиск. LocalLm отвечает за
локальные помощники задач и оценку токенов. `LocalAi.Cli` отвечает за регистрацию репозитория,
синхронизацию, совместимый транспорт и установку подключаемых хуков.

### Работа с результатами CodeSearch

`search_code` использует модель эмбеддингов, записанную в индексе. Production-поиск
получает измеренный порог релевантности для этой точной модели и завершается закрытым
отказом `threshold not calibrated`, если профиля нет; порог другой модели никогда не
заимствуется.

Что именно представляет собой результат — зависит от языка, и это стоит знать до чтения
выдачи. C# режется по синтаксису: отдельный чанк на тип — с полями и оглавлением членов —
и отдельный на каждый исполняемый член, то есть метод, конструктор, деструктор, оператор,
оператор преобразования, индексатор или свойство с настоящим телом. Поля и авто-свойства
едут внутри чанка типа, а не становятся собственными векторами: DTO с двадцатью свойствами
не должен превращаться в двадцать один вектор. Сгенерированный C# не индексируется вовсе.

TypeScript и Python тоже режутся по символам — по границам определений, которые сообщают
внешние индексаторы: чанк на определение с настоящим символом и точными строками, причём
определение, содержащее другие, перечисляет вложенные, а не повторяет их тела. Это работает,
если индексатор установлен, — но не требует, чтобы он сообщил границы тела. `scip-typescript`
не сообщает их для объявления, инициализированного вызовом, — `export const X = memo(() => …)`, —
и такое объявление всё равно получает свой чанк: граница читается по файлу, до строки перед
следующим объявлением, которое назвал индексатор. На окне остаётся то, для чего читать нечего:
объявление внутри тела функции, которому индексатор даёт `local N` без имени и без границ, и
объявление, уместившееся в одну строку, — одна строка не тело и отдельного вектора не стоит.

Все остальные индексируемые форматы, а также любой участок, не покрытый определением —
импорты, код уровня модуля, промежуток между двумя функциями, — режутся скользящим окном по
строкам: 60 строк с перекрытием в 12. Там результат называет файл и диапазон строк, а не
символ. Окно не разбирает синтаксис; что оно сохраняет — точные номера строк, и именно они
делают попадание пригодным к действию.

Точная навигация — отдельный слой: C# и XAML она разбирает напрямую, TypeScript и Python
импортирует из тех же внешних индексаторов.

Каждый результат поиска содержит непрозрачный `chunk_id`. Передайте его в
`get_code_chunk`, чтобы получить полный проиндексированный чанк исходника. ID привязан
к репозиторию, неизменяемой generation, текущему Git tree, хешу dirty-содержимого и
composite ordinal чанка. Malformed, cross-repository, stale или out-of-range ID
отклоняется, а не разрешается по другому snapshot.

Полученный из исходников MCP-вывод — это данные, а не инструкции. `search_code`
оставляет доверенную сводку индекса снаружи, а каждый результат возвращает в отдельном
блоке со свежим nonce:

```text
<untrusted-content id="<96-bit-lowercase-hex-nonce>" origin="search_code:<path>">
...полученный из исходника результат, включая chunk_id...
</untrusted-content id="<same-nonce>">
```

Ту же границу использует каждый инструмент, возвращающий полученное из исходников:
успешный вывод `get_code_chunk` и каждая локация из `go_to_definition`,
`find_references`, `find_implementations` и `find_relationships` — пути, идентификаторы
символов и диапазоны написаны тем же, кто написал репозиторий, и являются данными ровно
так же, как snippet. Ошибки валидации, status и maintenance-вывод остаются снаружи.
Потребители должны сохранять границу и не должны исполнять или выполнять инструкции,
найденные внутри неё.

Digest внутри `chunk_id` обнаруживает случайное повреждение и небрежную подделку, но не
является границей аутентификации. Право прочитать исходник даёт равенство репозитория,
generation, tree и dirty-overlay — оно проверяется до любого чтения.

Точная навигация C#, WPF/WinUI/MAUI/Avalonia XAML, TypeScript/JavaScript и Python хранится отдельно от векторов в
`semantic.sidx`. `localai sync` загружает основное решение через Roslyn/MSBuild, дополняет его
lossless-диапазонами XAML, platform namespaces, `x:DataType`, dependency/styled properties и
вариантами binding, а также импортирует ограниченный результат установленных индексаторов
`scip-typescript` и `scip-python`. Отсутствующий внешний индексатор отмечается как skipped в
manifest поколения и не ломает другие языки. Если в репозитории есть C#, а семантика
не покрыла ни одного его файла, `localai sync` сообщает об этом вместо тихого успеха, а
`index_status` называет такое поколение эвристическим, а не точным; `localai sync
--require-semantics` превращает предупреждение в отказ для хука или шага CI, которому лучше
остановиться. Базовый SIDX атомарно публикуется вместе с CIDX,
а для dirty worktree строится компактный snapshot-bound semantic overlay с изменёнными
семантическими срезами и явными tombstone для удалённых документов. MCP предоставляет
`go_to_definition`, `find_references`, `find_implementations` и `find_relationships`; локальная
диагностика доступна командами:

```powershell
localai semantic status --root C:\path\to\repository
localai semantic definition --path src/App.cs --line 10 --column 15 --root C:\path\to\repository
localai semantic references --path src/App.cs --line 10 --column 15 --root C:\path\to\repository
localai semantic implementations --path src/IService.cs --line 4 --column 18 --root C:\path\to\repository
localai semantic relationships --path src/Service.cs --line 8 --column 20 --direction outgoing --kind implementation --root C:\path\to\repository
localai semantic config init
localai semantic config show
localai semantic lsp-config init
localai semantic lsp-config show
localai semantic fallback-config init
localai semantic fallback-config show
localai semantic evaluate --cases tests/CodeSearch.Tests/Fixtures/SemanticNavigation/cases.json --root C:\path\to\repository
```

`--line` и `--column` считаются **с нуля**, а колонка — это смещение в UTF-16: так же, как у
MCP-инструментов. Строка 10 в редакторе — это `--line 9`. Позиция, на которой нет символа,
возвращает пустой результат, а не ошибку, поэтому промах на единицу выглядит как «здесь ничего
нет», а не как опечатка.

Глобальный файл `%LOCALAPPDATA%\LocalAi\semantic-indexing.json` настраивает включение
адаптеров, пути и аргументы executables, выходные файлы, timeout, лимит вывода процессов и все
лимиты SCIP parser. Policy доверенного адаптера также явно задаёт legacy fallback позиций:
`Utf16` для `scip-typescript` и `Utf32` для `scip-python`; прямой импорт SCIP без такой policy
по-прежнему отклоняет неоднозначный индекс. Файл читается при каждом sync, поэтому изменение не
требует пересборки LocalAi. На Windows npm `.cmd` shims разрешаются через PATH/PATHEXT с
сохранением лимита вывода, timeout и завершения дерева процессов.

Live-навигация включается явно через `%LOCALAPPDATA%\LocalAi\language-servers.json`; изменение
этого файла не требует пересборки LocalAi. В нём настраиваются общий переключатель,
executable/arguments по языкам, таймауты запросов и shutdown, максимальный размер JSON-RPC и
ограниченный stderr. `localai semantic lsp-config init` создаёт отключённые по умолчанию шаблоны
для TypeScript/JavaScript, Python, HTML и C#. MCP-клиент передаёт версии через
`lsp_open_document` и закрывает документ через `lsp_close_document`; пока документ открыт,
точные ответы LSP для definition/reference/implementation имеют приоритет, после close используется snapshot-bound SIDX. При обрыве
transport сервер один раз перезапускается и получает все открытые документы с последними version
и полным text до повторения запроса. Repository-relative пути на всех ОС нормализуются через `/`;
нативные абсолютные пути существуют только на границе LSP `file://`.
Постоянные запросы implementations и relationships используют точный граф SIDX. Направление
incoming находит реализации, overrides и производные типы; outgoing возвращает соответствующие
базовые или интерфейсные символы. Для relationship-запросов эвристический текстовый поиск не
используется.

Если ни LSP, ни snapshot-bound SIDX не разрешили символ, LocalAi может выполнить ограниченный
syntax/text-поиск. Файл `%LOCALAPPDATA%\LocalAi\semantic-navigation.json` включает этот fallback
и задаёт лимиты числа файлов, размера файла, числа результатов, длины идентификатора и режим
регистра. Он читается при каждом запросе, поэтому изменение не требует пересборки. Результаты
fallback всегда имеют точность `Heuristic` и никогда не повышаются до precise.

Fallback также объясняет свою причину. `go_to_definition` и `find_references` предваряют
эвристический ответ строкой `semantic_navigation_degraded:` с названием причины, и две причины
сформулированы по-разному, потому что re-sync лечит только одну: поколение, опубликованное до
появления семантической индексации, вообще не содержит `semantic.sidx`, тогда как актуальный
индекс может просто не иметь символа в этой позиции. `index_status` сообщает то же состояние в
строке `Navigation:` — потому что больше ничто там не меняется: репозиторий, не пересобранный с
момента появления семантической индексации, по-прежнему показывает правильную модель, отсутствие
дрейфа коммита и `Status: current`, пока каждый навигационный запрос отвечает текстовым поиском.
Marker-based команда `semantic evaluate` выводит correctness, process-cold время загрузки,
перцентили первого/warm запроса, наблюдаемую разницу памяти и общий размер base/overlay SIDX.
Текущий baseline описан в
[semantic-navigation-evaluation.ru.md](docs/semantic-navigation-evaluation.ru.md).

Эквивалентные команды CLI:

```powershell
dotnet run --project src/CodeSearch.Cli -- search --query "where is the broker queue" --root C:\path\to\repository
dotnet run --project src/CodeSearch.Cli -- get-chunk --id "<chunk_id>" --root C:\path\to\repository
```

Измеренный откалиброванный профиль, происхождение корпуса, результаты A/B, эвристика
токенов и ограничения приведены в
[оценке retrieval CodeSearch](docs/codesearch-evaluation.ru.md).

Сводный перечень всех двадцати инструментов MCP — что существует, зачем и какое ограничение
важно — в [справочнике инструментов](docs/mcp-tools.ru.md).

Связный разбор для тех, кто пользуется этим каждый день — что делает, как устроено, чем
защищено и сколько это экономит — в [обзоре для разработчиков](docs/overview.ru.md).

## Установка

Скачайте `LocalAi.Installer.exe` из
[последнего релиза](https://github.com/MrAliev/local-ai-developer-toolkit/releases/latest) и
запустите. Ничего готовить заранее не нужно: ни аккаунта GitHub, ни входа, ни клона этого
репозитория. Единственное, без чего мастер не пойдёт дальше, — Windows 10 или 11 на x64.

Установщик не подписан Authenticode, поэтому Windows покажет *«Windows protected your PC»*.
Выберите **More info → Run anyway**. SHA-256 файла публикуется в заметках релиза — если файл
передали вам, есть с чем сверить.

Мастер проводит по тому, что найдено на машине; какие предварительные требования ставить (Git,
Ollama и индексаторы языков — всё через winget, каждое с отдельного согласия); какой релиз
устанавливать; какие локальные модели помещаются в видеокарту; какие AI-клиенты подключить.
Ничего не применяется до страницы подтверждения.

Больше всего времени и места займут модели: машина с 16-гигабайтным адаптером, где не стоит ни
одной, скачает десятки гигабайт. Кнопка **Skip** на странице моделей ставит только инструменты,
а модели можно добавить позже, попросив подключённого клиента выполнить `local_models_sync`.

### Установка из папки

Исполняемый файл — это мастер, а продукт — пакет, который он скачивает по ходу установки. Чтобы
поставить LocalAi на машину без доступа к GitHub, скачайте `release-manifest.json`,
`release-manifest.sig` и `localai-package.zip` там, где доступ есть, положите все три в одну
папку и укажите её мастеру — либо путём на странице пакета, либо положив
`LocalAi.Installer.exe` рядом с ними: такую папку мастер предложит сам.

Папка не даёт доверия. Манифест проверяется по ключу, встроенному в установщик, а пакет — по
SHA-256 из этого манифеста, ровно как при загрузке по HTTPS. Модели по-прежнему берутся из
реестра Ollama, поэтому установка становится возможной без GitHub, но не без интернета.

## Предварительные требования

Это требования для сборки решения из исходников. Для установки готового релиза не нужно ничего
из перечисленного.

- .NET 10 SDK
- Доступ к `https://api.nuget.org/v3/index.json` для чистого восстановления
- Ollama и установленная совместимая модель только при работе с функциями, поддерживающими модели; сборки и модульные тесты не запускают Ollama

`NuGet.config` намеренно очищает наследуемые источники пакетов, чтобы незатронутые частные фиды не становились зависимостями сборки.

## Быстрый старт

Восстановите и проверьте решение:

```powershell
dotnet restore LocalAi.slnx
dotnet test LocalAi.slnx --configuration Release --no-restore
```

Проверьте синхронизацию репозитория без применения внешнего настройки:

```powershell
dotnet run --project src/LocalAi.Cli -- bootstrap --dry-run
dotnet run --project src/LocalAi.Cli -- repo status
```

Синхронизируйте авторизованный репозиторий явно:

```powershell
dotnet run --project src/LocalAi.Cli -- sync --root C:\path\to\repository
```

После установки двоичных файлов MCP проверьте и синхронизируйте модельный каталог через
LocalLm:

```text
local_models_status
local_models_sync
```

Команда синхронизации отправляет разрешенные задачи обслуживания в одну и ту же устойчивую очередь. Она не предоставляет общую команду для извлечения и не удаляет существующие модели.

Установите общий цепочный Git hook только после одобрения внешнего изменения:

```powershell
C:\path\to\LocalAi\bin\launcher\localai-launcher.exe run localai hooks install --root C:\path\to\repository
```

Хуки ставятся туда, куда Git на самом деле смотрит. Если в репозитории задан `core.hooksPath`
— а его задают husky, lefthook и simple-git-hooks, — они пойдут туда, а не в `$GIT_DIR/hooks`,
откуда Git их читать не будет. Для husky это каталог `.husky`, а не `.husky/_`, который husky
переписывает заново при каждом `npm install`. Хуки, попавшие в рабочее дерево, дописываются в
`.git/info/exclude`: так они не появляются в `git status`, и при этом мы не трогаем
`.gitignore`, который принадлежит репозиторию, а не нам.

## Что настраивает установщик

Законченная установка должна оставлять машину, которой действительно можно пользоваться,
поэтому мастер делает три вещи помимо копирования исполняемых файлов. Каждая показана на
странице подтверждения, и ни одна не применяется до того, как запуск подтверждён.

**Интеграция клиентов.** Для каждого обнаруженного AI-клиента мастер регистрирует MCP-серверы
`codesearch` и `locallm` на стабильный путь launcher и записывает управляемый блок инструкций в
глобальный файл инструкций этого клиента — `~/.claude/CLAUDE.md` для Claude, `~/.codex/AGENTS.md`
для Codex. Для обнаруженного клиента по умолчанию выбрано и то, и другое; ненайденный клиент
остаётся нетронутым, а явный выбор пользователя всегда важнее умолчания.

Для Codex каждый управляемый сервер дополнительно получает
`default_tools_approval_mode = "approve"`, а каждый инструмент обоих серверов — строку
`[mcp_servers.<server>.tools.<tool>]` с тем же значением. `approve` — это значение, которое
пропускает запрос: Codex переводит его в «одобрение не требуется»; спрашивает значение
`prompt`.

Нужно и то, и другое. Codex разрешает режим как per-tool override, затем серверный дефолт,
затем `auto`, а под `auto` инструмент без объявленных аннотаций требует подтверждения — а это
все инструменты здесь. Серверный дефолт покрывает инструменты из будущих релизов сразу, как
только они появятся; строки делают текущий набор явным.

Записывается только отсутствующее. Существующая строка или серверный дефолт остаются ровно
такими, какими их написал пользователь, включая запрещающие инструмент, — это решение, которое
надо сохранить, а не упущение, которое надо исправить. Без этого машина накапливает строки
только для тех инструментов, к которым обращались: на одной долгоживущей установке было
одиннадцать строк из двадцати. Список привязан к серверам тестами, которые читают их атрибуты
рефлексией, поэтому новый инструмент не может выйти без своей строки.

Блок намеренно не привязан к репозиториям. Индексация остаётся опциональной для каждого
репозитория, поэтому вместо предположений о том, что было настроено на машине сборки, блок
называет проверку (`localai repo status`) и две команды, подключающие любой репозиторий там,
где он склонирован. Без этого клиент на чистой машине умеет пользоваться локальными моделями,
но ни разу не предложит проиндексировать первый же открытый репозиторий.

Блок ограничен маркерами `<!-- BEGIN LOCALAI MANAGED INSTRUCTIONS -->` и парным ему конечным.
Всё между ними заменяется целиком при следующей установке, всё снаружи сохраняется, поэтому
обновление правил никогда не стоит пользователю его собственных заметок. Блок описывает
маршрутизацию — когда браться за `search_code` вместо текстового поиска, что относится к
`read_image`, `triage_log` и `ask_local` — и транспортные инварианты: только общий broker,
никогда Ollama напрямую, проверка полной резидентности в VRAM. Одних транспортных правил на
практике оказалось мало: они описывают, как вызов обязан идти, но не говорят, когда его вообще
делать, и агент, прочитавший только их, продолжает работать в облаке, пока установленные
инструменты простаивают.

**Модели.** Мастер устанавливает модели, которые машина способна удержать, и пропускает уже
установленные: список установленного спрашивается у broker, а не предполагается, и скачивается
только недостающее. Кандидаты берутся из подписанного манифеста релиза, а не из каталога
маршрутизации или реестра, потому что установщик моделей отвергает всё, что не может сверить с
подписью. Автоматический выбор берёт наибольший контекст, в который модель помещается; модель,
не влезающую в адаптер ни при каком контексте, он называет по имени, а не скачивает, чтобы
оставить непригодной. Скачанное проходит preflight, поэтому модель, не загрузившаяся целиком в
видеопамять, попадает в отчёт, а не принимается молча.

Раз выбор определяется манифестом, релиз, опубликованный без списка моделей, не установит ни
одной, — именно это и происходило с 0.1.29 по 0.1.44: `--models` был необязательным, а скрипт
публикации его не передавал. Теперь список строится под каждый релиз из каталога маршрутизации
и публичного реестра, и подпись его требует:

```powershell
localai-release-signer models --out publish\release\release-models.json
localai-release-signer sign --models publish\release\release-models.json ...
```

`sign` не запускается без `--models` (или явного `--no-models`), `release --publish` отвергает
манифест с пустым списком, а мастер считает запуск, не установивший ни одной модели, ошибкой, а
не строкой в логе. Размеры читаются из реестра в момент подписи, а не хранятся в репозитории:
тег, переизданный с другим квантованием, имени не меняет, поэтому размер в исходниках устаревает
незаметно.

**Политика резидентности.** Страница видеопамяти пишет `%LOCALAPPDATA%\LocalAi\policy.json` —
но только если LocalAi установлен. Эта запись создаёт родительский каталог с унаследованными
правами, а аренда раскладки такой корень отвергает и не имеет права его чинить, поэтому запуск,
упавший до установки пакета, оставлял после себя каталог, из-за которого каждая следующая
установка отказывала себе сама со словами «the directory still inherits access rules».
Ослабление политики — сознательное действие, и оно остаётся видимым в отчёте о запуске.

**Ни аккаунта, ни входа.** Релизы публичные, поэтому установщик скачивает манифест, подпись и
пакет по обычному HTTPS вообще без учётных данных. Аккаунт GitHub для установки не нужен, а
GitHub CLI перечислен среди необязательных предварительных требований, а не обязательных.

CLI остаётся запасным путём и пробуется только тогда, когда анонимный не сработал: форк,
оставленный приватным, по-прежнему устанавливается через вход, который у его владельца уже есть,
а сеть, блокирующая хост релизов, может не блокировать API. Отказ проверки подписи через второй
транспорт не переспрашивается никогда: манифест, не прошедший встроенный ключ, — это не проблема
транспорта, а запрашивать один и тот же документ у разных каналов, пока какой-нибудь не поверят,
— это ровно та атака, которую эта схема и отвергает.

Чего транспорт не делает никогда — так это не даёт доверия. Манифест сверяется с ключом,
вшитым в установщик, а пакет — с SHA-256 из этого манифеста, каким бы путём они ни пришли.
Анонимная загрузка подписанного документа ровно настолько же надёжна, насколько
аутентифицированная.

## Сборка и тестирование

Из корня репозитория:

```powershell
dotnet restore LocalAi.slnx
dotnet build LocalAi.slnx --configuration Release --no-restore
dotnet test LocalAi.slnx --configuration Release --no-build
```

Базовая верификация с использованием одной команды выглядит так:

```powershell
dotnet test LocalAi.slnx --configuration Release
```

## Публикация исполняемых файлов

Публикуйте только те проекты, которые являются исполняемыми и необходимы:

```powershell
dotnet publish src/CodeSearch.Cli/CodeSearch.Cli.csproj --configuration Release --output publish/CodeSearch.Cli
dotnet publish src/CodeSearch.Mcp/CodeSearch.Mcp.csproj --configuration Release --output publish/CodeSearch.Mcp
dotnet publish src/LocalLm.Mcp/LocalLm.Mcp.csproj --configuration Release --output publish/LocalLm.Mcp
dotnet publish src/LocalAi.Cli/LocalAi.Cli.csproj --configuration Release --output publish/LocalAi.Cli
dotnet publish src/LocalAi.Launcher/LocalAi.Launcher.csproj --configuration Release --output publish/LocalAi.Launcher
dotnet publish src/LocalAi.Broker/LocalAi.Broker.csproj --configuration Release --output publish/LocalAi.Broker
```

> Очередь и bootstrap runtime ACL живут в библиотеке `LocalAi.Broker.Core`, и
> `LocalAi.Broker.Client` ссылается на неё, а не на исполняемый проект брокера. Так и
> держите. Пока клиент ссылался на проект с `OutputType=Exe`, публикация любого
> зависимого проекта выносила ещё и `apphost.exe`, `deps.json` и `runtimeconfig.json`
> брокера, и RID-специфичная framework-dependent публикация падала с `NETSDK1152` —
> очистка `bin`/`obj` не помогала, потому что коллизия рождалась в процессе сборки.
>
> Та же ссылка попутно клала `LocalAi.Broker.exe` рядом с зависимыми проектами, и именно
> на этом держался запуск брокера при разработке. Теперь копирование объявлено явно, в
> `src/BrokerBinary.props`, и применяется к тем проектам, которым это нужно.

Для автономного установщика (без зависимости от системного .NET runtime):

```powershell
dotnet publish src/CodeSearch.Cli/CodeSearch.Cli.csproj --configuration Release --runtime win-x64 --self-contained true --property:PublishSingleFile=true --output publish/CodeSearch.Cli
dotnet publish src/CodeSearch.Mcp/CodeSearch.Mcp.csproj --configuration Release --runtime win-x64 --self-contained true --property:PublishSingleFile=true --output publish/CodeSearch.Mcp
dotnet publish src/LocalLm.Mcp/LocalLm.Mcp.csproj --configuration Release --runtime win-x64 --self-contained true --property:PublishSingleFile=true --output publish/LocalLm.Mcp
dotnet publish src/LocalAi.Cli/LocalAi.Cli.csproj --configuration Release --runtime win-x64 --self-contained true --property:PublishSingleFile=true --output publish/LocalAi.Cli
dotnet publish src/LocalAi.Launcher/LocalAi.Launcher.csproj --configuration Release --runtime win-x64 --self-contained true --property:PublishSingleFile=true --output publish/LocalAi.Launcher
dotnet publish src/LocalAi.Installer/LocalAi.Installer.csproj --configuration Release --runtime win-x64 --self-contained true --property:PublishSingleFile=true --output publish/LocalAi.Installer
```

Каталог `publish/` игнорируется. Публикация не регистрирует исполняемые файлы с клиентом AI или устанавливает Git хуки.

### Выпуск релиза

```powershell
dotnet run --project src/LocalAi.ReleaseSigner/LocalAi.ReleaseSigner.csproj -c Release -- `
    release --version 0.1.36 --root .
```

Первый запуск создаёт заготовки `docs/releases/0.1.36.md` и `.ru.md` и останавливается. Заметки
пишутся руками: это связный текст о том, что изменилось и почему, а список тем коммитов — не
худший его вариант, а совсем другая и куда менее полезная вещь. Второй запуск, когда заметки
готовы, открывает pull request с ними.

После того как этот PR смержен, из `main`:

```powershell
dotnet run --project src/LocalAi.ReleaseSigner/LocalAi.ReleaseSigner.csproj -c Release -- `
    release --version 0.1.36 --root . --publish
```

Это собирает, подписывает локально, сверяет манифест с выпускаемым релизом и только потом ставит
тег и загружает ассеты. Команда отказывается работать при грязном дереве, на ветке не `main`, на
коммите, которого нет у `origin`, на коммите без зелёного прогона `build-and-test`, на уже
занятой версии и на заметках, где ещё стоит TODO из заготовки.

Разделение сделано намеренно. Сборку и подпись можно повторять сколько угодно; тег и публикация —
то, что резолвят чужие установщики, поэтому это отдельный явный вызов, а не следующая стадия того
же самого. Мерж PR во всех случаях остаётся человеческим решением.

Обе половины существуют потому, что одна и та же версия раньше набиралась в четырёх местах — оба
имени файлов заметок, скрипт сборки, тег, — а коммит в двух: version directory в манифесте и то,
куда в итоге указал тег. Ничто их не сверяло, а расхождение давало пакет, который проходит
проверку подписи, ставится и не является тем, что назвал тег.

### Подпись манифеста релиза

Установленной машине не нужен системный .NET. Все компоненты, включая брокер, поставляются
самодостаточными исполняемыми файлами, а брокер запускается как `LocalAi.Broker.exe` из
каталога того компонента, которому он понадобился. Путь берётся через
`AppContext.BaseDirectory`; **`Assembly.Location` использовать нельзя** — в single-file
публикации он возвращает пустую строку, и процесс умирает с «The path is empty», так и не
дойдя до брокера.

Установщик отвергает любой релиз, чей манифест не подписан доверенным ключом ECDSA P-256.
Это самостоятельно сгенерированная пара ключей, а не сертификат: ни центра сертификации,
ни расходов здесь нет. Создайте её один раз и держите приватный ключ вне репозитория:

```powershell
$dir = "$env:LOCALAPPDATA\LocalAi\release-signing"
New-Item -ItemType Directory -Force -Path $dir | Out-Null
$ec = [System.Security.Cryptography.ECDsa]::Create(
    [System.Security.Cryptography.ECCurve]::CreateFromFriendlyName("nistP256"))
[IO.File]::WriteAllBytes("$dir\release-signing-private.pkcs8.der", $ec.ExportPkcs8PrivateKey())
[IO.File]::WriteAllBytes("$dir\release-signing-public.spki.der", $ec.ExportSubjectPublicKeyInfo())
$ec.Dispose()
icacls $dir /inheritance:r /grant:r "$($env:USERNAME):(OI)(CI)F"
```

Сначала соберите пакет. Верификатор сверяет состав архива с
`LocalAiPackageLayout.PackageArtifactFiles` через `SetEquals`, поэтому в нём должно быть
ровно шесть артефактов плюс `localai-package.json` — плоско и без единой лишней записи.
**Именно поэтому исполняемые файлы обязаны публиковаться self-contained**: в формате нет
места для соседних сборок-зависимостей, и framework-dependent сборку выложить релизом
нельзя.

```powershell
localai-release-signer pack `
    --input publish\artifacts `
    --release-version 0.1.22 `
    --version-directory d9c52d2 `
    --out publish\release\localai-package.zip
```

Соберите список моделей, который понесёт манифест. Размеры берутся из публичного реестра
моделей, поэтому шаг требует сети и падает, а не пропускает тег, размер которого не
прочитал:

```powershell
localai-release-signer models --out publish\release\release-models.json
```

Затем подпишите пакет утилитой `localai-release-signer`. Она собирает манифест тем же
каноническим сериализатором, которым пользуется проверяющий, приводит подпись к
канонической форме low-S, обязательной для верификатора, и перепроверяет результат ещё до
публикации:

```powershell
localai-release-signer sign `
    --package publish\release\localai-package.zip `
    --package-uri https://github.com/MrAliev/local-ai-developer-toolkit/releases/download/0.1.22/localai-package.zip `
    --release-version 0.1.22 `
    --version-directory d9c52d2 `
    --models publish\release\release-models.json `
    --out publish\release
```

`--models` не является необязательным. Релиз, подписанный без него, не установит ни
одной модели, поэтому утилита отказывается работать, пока список не передан или пока
`--no-models` не скажет, что пропуск сделан намеренно.

Перед публикацией прогоните готовые артефакты через собственный верификатор пакета из
установщика. Он выполняет тот же разбор архива, распаковку и сверку метаданных, что и
установка, поэтому структурная ошибка вылезет здесь, а не на чужой машине:

```powershell
localai-release-signer verify-package `
    --package publish\release\localai-package.zip `
    --manifest publish\release\release-manifest.json `
    --signature publish\release\release-manifest.sig
```

Проверка выполняется публичным ключом, встроенным в `LocalAi.Installer.Core`
(`ReleaseTrustAnchor`), а не файлом рядом с утилитой. Поэтому подпись ключом, которому
выпущенный установщик не доверяет, падает сразу, а не на машине пользователя. При провале
проверки на диск не пишется ничего. Публичную половину держите в репозитории по пути
`src/LocalAi.Installer.Core/Releases/release-signing-public.spki.der`, приватная не должна
попадать в репозиторий никогда.

Публикуйте `release-manifest.json` и `release-manifest.sig` ассетами релиза рядом с
пакетом. Правила полей, о которые проще всего споткнуться: `ReleaseVersion` — строгий
semver **без префикса `v`**, `PackageSha256` — hex **в верхнем регистре**, `PackageUri` —
только `https`, без userinfo и фрагмента.

### Authenticode: два разных механизма

Authenticode встречается в проекте дважды, на разных уровнях, и их легко перепутать.

**Сам установщик.** `LocalAi.Installer.exe` публикуется без подписи. Именно этот файл
человек скачивает и запускает, поэтому его и оценивает Windows: неподписанный
исполняемый файл с меткой загрузки из интернета показывает полноэкранное «Windows
protected your PC», которое обходится через *More info -> Run anyway*. Убрать это
предупреждение может только подпись самого файла — ничто внутри релиза на него не влияет.
Пока сертификата нет, `release --publish` печатает SHA-256 установщика, чтобы у того, кто
передаёт файл дальше, было с чем его сверить.

**Файлы внутри пакета.** `RequiresAuthenticode` в подписанном манифесте — совсем другая
проверка, её выполняет установщик, а не Windows: при включённом флаге каждый `.exe` и
`.dll`, извлечённый из пакета, обязан быть подписан издателем из
`AuthenticodePublisherPolicy`, и файл без подписи установку останавливает. Сейчас эта
политика — заглушка: subject `CN=LocalAi` и SHA-256 из шестидесяти четырёх нулей, которой не
может соответствовать ни один файл. Поэтому `--require-authenticode` отказывается работать,
пока она такова, вместо того чтобы выпустить релиз, который любая машина отвергнет как
несовпадение подписи; установщик, встретивший такой манифест, говорит то же самое, а не
винит пакет. Чтобы включить флаг, замените оба значения на настоящее имя подписанта и
SHA-256 его SubjectPublicKeyInfo (не отпечаток сертификата).

**Порядок, когда сертификат появится.** Подписывать артефакты нужно до упаковки, а не
после: `pack` хеширует архив, а `sign` — пакет, поэтому подпись бинарника задним числом
делает недействительными оба. Последовательность такая: публикация, `signtool` по
`publish\artifacts` и по `LocalAi.Installer.exe`, затем `pack`, `models`, `sign`,
`verify-package`. Обязательно ставьте метку времени (`signtool /tr`) — иначе все подписи
перестанут проверяться в день истечения сертификата.

### Неизменяемые версии и атомарная активация

Опубликуйте CLI, MCP-серверы, брокер, контракты и их зависимости среды выполнения
в новый промежуточный каталог. Сливайте выход публикации всех проектов в этот один
каталог **вместе с подкаталогом `runtimes\`** — framework-dependent публикация без явного
RID кладёт windows-специфичные сборки вроде `System.Diagnostics.EventLog.dll` в
`runtimes\win\lib\net10.0\`, и `deps.json` разрешает их именно по этому пути. Плоское
копирование только верхнего уровня даёт каталог, который падает на старте с
`FileNotFoundException`.

Проверьте полноту результата до активации: в промежуточном каталоге должны быть все
исполняемые файлы компонентов (`codesearch.exe`, `codesearch-mcp.exe`, `locallm-mcp.exe`,
`localai.exe`, `localai-launcher.exe`, `LocalAi.Broker.exe`) и все зависимости
(`CodeSearch.Core.dll`, `LocalLm.Core.dll`, `LocalAi.Broker.Client.dll`,
`LocalAi.Repository.dll`, `ModelContextProtocol.dll`, набор `Microsoft.Extensions.*`).
Каталог из пары десятков файлов — это неполная публикация, а не релиз: запуск инструмента
из него падает с ошибкой загрузки `System.Runtime`, и приходит она от дочернего процесса,
а не от launcher.

Затем один раз скопируйте
его в `bin\versions\<version>`. Каталог опубликованной версии неизменяем: активация
никогда не обновляет и не удаляет его, а исторические версии остаются доступными для
отката.

Отдельно опубликованный launcher установите по пути
`bin\launcher\localai-launcher.exe`. Codex, Claude, Git hooks и обёртки делегирования
должны регистрировать этот стабильный исполняемый файл вместе с префиксом инструмента,
например:

```text
localai-launcher.exe run codesearch-mcp
localai-launcher.exe run locallm-mcp
localai-launcher.exe run localai
```

Активная версия хранится в атомарно заменяемом документе `bin\current.json`:

```json
{"schemaVersion":1,"version":"<version>"}
```

После проверки каталога кандидата активируйте его. Активация всегда требует явного
ожидания относительно заменяемого указателя, чтобы параллельная активация не была
затёрта молча:

```powershell
# Замена существующего указателя: укажите его текущий SHA-256.
$expected = (Get-FileHash bin\current.json -Algorithm SHA256).Hash
bin\launcher\localai-launcher.exe activate <version> --if-current-sha256 $expected

# Самая первая активация, когда bin\current.json ещё не существует.
bin\launcher\localai-launcher.exe activate <version> --if-current-missing

# Добавьте --stop-running, если предыдущая версия ещё используется.
bin\launcher\localai-launcher.exe activate <version> --if-current-sha256 $expected --stop-running
```

Вызов без обоих флагов — ошибка использования, код возврата 2. Активация также
завершается ошибкой, если наблюдаемый указатель не совпал с заявленным ожиданием. Без
`--stop-running` она завершается ошибкой, пока версия используется процессом, запущенным
через launcher; с этим флагом останавливаются только процессы, чей точный путь
исполняемого файла или свежая идентичность сборки брокера относится к предыдущей версии,
после чего указатель переключается. Она не останавливает Ollama или несвязанные процессы
`dotnet`. Для отката активируйте ранее проверенный неизменяемый каталог. Все запросы к моделям, включая
команды совместимости, по-прежнему проходят через общий FIFO-брокер; прямой доступ к
Ollama не поддерживается.

Остановка инструментов версии доступна отдельной командой:

```powershell
bin\launcher\localai-launcher.exe stop [--version <version>]
```

Брокера при этом просят завершиться, а не убивают. Рядом с `host.json` пишется запрос на
остановку с его PID и временем старта; брокер замечает его на том же heartbeat, который и так
публикует раз в секунду, перестаёт арендовать новые задачи, доводит текущую до конца и выходит
сам. Убивают только того, кто не ушёл до дедлайна: у брокера долговечная очередь, и он может
быть на середине инференса, поэтому уничтожение — последний ответ, а не механизм. Запрос
адресован не только по PID, но и по времени старта, чтобы устаревший не погасил здорового
брокера, унаследовавшего номер, и удаляется сразу после того, как принят.

У stdio-инструментов нет ни долговечного состояния, ни канала, чтобы их попросить, а клиент
поднимает их по требованию — для них жёсткое завершение единственный и достаточный способ.

Она останавливает процессы, работающие из активной версии — или из названной, — и указателя
не трогает. Активация останавливает их тоже, но только в рамках переключения, а шагом раньше
подменяется сам стабильный launcher. Перезаписать работающий exe Windows не даёт, и каждый
подключённый клиент держит по launcher-процессу на инструмент, поэтому установка могла
опубликовать новую версию, не суметь положить новый launcher и откатить всё целиком — при том
что лекарство лежало шагом дальше. Теперь установщик останавливает инструменты тогда и только
тогда, когда они мешают: обновление на простаивающей машине не трогает ничего.

Чтение указателя берёт **разделяемую** аренду, эксклюзивную — только сама подмена. Это важно:
каждый запущенный через launcher инструмент держит разделяемую аренду всё время жизни, поэтому
на машине, где LocalAi действительно используется, её всегда держит подключённый клиент, — и
установщик, бравший блокировку эксклюзивно просто ради чтения указателя, отказывал в
обновлении, не дойдя до активации, которая эти процессы и остановила бы. Чтению эксклюзивность
никогда не требовалась: подмену по-прежнему защищает `--if-current-sha256`, который ловит
изменившийся между чтением и активацией указатель, а не затирает его.

### Политика резидентности моделей

По умолчанию модель обязана **целиком помещаться в видеопамять**, всё остальное
отвергается. Это не настройка производительности. Модель, сползшая в системную память, не
падает — она просто становится в разы медленнее, и в ответе об этом ничего не сказано.

На машинах без пригодного дискретного адаптера политику можно ослабить:

```powershell
localai policy show
localai policy set --residency AllowPartialOffload
localai policy set --residency AllowCpu
localai policy set --idle-model-keep-alive-seconds 0
```

| Значение | Допускает | Отвергает |
| --- | --- | --- |
| `RequireFullVram` (по умолчанию) | полностью резидентную загрузку | всё остальное |
| `AllowPartialOffload` | часть модели на адаптере | чистую загрузку на CPU |
| `AllowCpu` | всё, что реально загрузилось | модель с нулевым размером |

Политика лежит в `%LOCALAPPDATA%\LocalAi\policy.json` и читается одинаково брокером, CLI и
установщиком. Отсутствующий, повреждённый или неизвестный по значению документ откатывается
к `RequireFullVram`: ошибка разбора не должна молча ослаблять проверку. Уже запущенный
брокер сохраняет прежнюю политику до перезапуска.

`IdleModelKeepAliveSeconds` задаёт, сколько секунд broker удерживает резидентную модель после
перехода в простой. Значение по умолчанию — `0`: модель выгружается сразу, если в очереди нет
работы, маршрутизированной на эту же модель. Задания для других моделей её не удерживают.
Задать другой интервал можно командой
`localai policy set --idle-model-keep-alive-seconds <секунды>`.

Деградация остаётся видимой: каждая загрузка ниже полной резидентности несёт предупреждение
с долей, которая дошла до видеопамяти, а `FullyResident` сообщает правду, а не константу.
Учтите, что блок инструкций для агентов по-прежнему требует full-VRAM валидации, поэтому
ослабление политики расходится с тем, что эти инструкции обещают.

NPU здесь не помогает. Всё идёт через Ollama, а её бэкенды — CPU, CUDA, ROCm, Metal и
Vulkan; NPU программируются отдельным стеком (OpenVINO, DirectML, Windows ML), и показатели
резидентности, на которых построена эта политика, к памяти NPU неприменимы.

### Удержание данных

Всё, что производит runtime, ограничено, а сами границы лежат в
`%LOCALAPPDATA%\LocalAi\retention.json`. Отсутствующий или испорченный документ даёт значения
по умолчанию.

| Граница | По умолчанию | К чему применяется |
| --- | --- | --- |
| `ResponseGraceMinutes` | 10 | пол: тело ответа моложе этого срока не удаляется никогда |
| `ResponseRetentionMinutes` | 60 | возраст, в котором тело архивного ответа удаляется |
| `ResponseBudgetBytes` | 512 МБ | суммарный объём хранимых тел, первыми уходят старые |
| `ArchiveRetentionDays` | 14 | возраст, в котором архивная запись удаляется целиком |
| `ArchiveEntryLimit` | 2000 | число хранимых архивных записей, первыми уходят старые |
| `GenerationsPerRepository` | 3 | поколений индекса на репозиторий, включая текущее |
| `InstalledVersions` | 3 | каталогов версий в `bin\versions` |
| `LauncherBackups` | 3 | резервных копий launcher в `installer\backups` |
| `TelemetryRetentionDays` | 30 | возраст, в котором удаляется запись телеметрии задания в `telemetry\metrics` |

Архив — это передача ответа, а не журнал: клиент забирает результат на следующем опросе после
перехода задания в терминальное состояние и больше к нему не возвращается, но один батч
эмбеддингов оставляет за собой мегабайты векторов. Брокер применяет архивные границы
постоянно — не чаще раза в минуту и с ограничением на объём работы за проход, поэтому очередь
не простаивает за уборкой — и удаляет тело, сохраняя request и state как след того, что
выполнялось.

`ResponseGraceMinutes` — единственное значение здесь, которое защищает корректность, а не
диск. Ни одна из остальных границ не может удалить тело внутри этого окна, а документ,
отредактированный вручную в ноль, зажимается, а не исполняется.

Чтобы разобрать накопившееся или дотянуться до того, чем фоновый проход не владеет:

```powershell
localai prune --dry-run
localai prune
```

`prune` никогда не удаляет поколение, на которое указывает манифест репозитория, версию из
`bin\current.json` и задание, пробывшее в терминальном состоянии меньше grace. Версия, чьи
файлы всё ещё загружены работающим инструментом, удалиться откажется — и это правильный
ответ, её заберёт следующий прогон. Он же забывает записи репозиториев, чей checkout больше не
существует: именно такой след оставляет за собой одноразовый worktree.

### Совместимость брокера и запуск

Схема 3 файла `host.json` брокера публикует явную версию протокола и стабильное семейство
совместимости сборки. Путь к его assembly остается диагностической записью и записью
launcher о владении версией; работоспособность клиента не зависит от совпадения путей DLL.
Поэтому совместимые установленные и разрабатываемые клиенты используют один
общесистемный брокер даже при разных путях DLL.

Свежий работающий несовместимый или устаревший host остается запущенным, но наблюдающий
клиент сразу получает `broker_incompatible` и не пытается запустить второй брокер. При
запуске ранний ненулевой код
завершения дочернего процесса дает `broker_start_failed`, а ограниченное ожидание без
наблюдения совместимого работоспособного состояния дает `broker_start_timeout` с
последним наблюдением. Дочерний процесс с нулевым кодом завершения, вероятно, проиграл
`broker.lock` другому процессу, поэтому клиент наблюдает состояние host владельца этой
блокировки только в тех же границах. CodeSearch сохраняет лексический fallback для
`broker_start_timeout`.

Эти правила не изменяют singleton FIFO, runtime ACL, активацию неизменяемых версий,
full-VRAM/zero-offload или запрет прямого доступа к Ollama.

## Модельно-ориентированное маршрутизирование

Встроенный каталог `model-routing.json` сопоставляет каждый локальный профиль задачи с одной или несколькими подходящими моделями. Профили охватывают обычный и технический перевод, а также перевод изображений; OCR и визуальный анализ; векторные эмбеддинги и детерминированный точный поиск; анализ, редактирование, ревью и переранжирование кода; анализ журналов; извлечение и классификацию; краткие сводки, синтез по нескольким файлам и планирование.

Брокер соблюдает эти инварианты:

- Уровни контекста диапазонируют от 2К до официального максимального для каждой модели (до 256К). Уровень может использоваться только тогда, когда предварительная проверка показывает, что полный раннер укладывается в VRAM.
- Холодная модель проходит предварительную проверку с пустым содержимым перед отправкой реальной задачи.
- `/api/ps` должен сообщать `size_vram == size`; отключение CPU или системной RAM вызывает именно эту комбинацию модели/контекста и запускает установленный резервный вариант.
- Планировщик предпочитает совместимую работу для резидентной модели, фиксирует каждый выбранный снимок, упорядочивает его задачи по прогнозируемой длительности, ждёт до двух секунд для сбора связанной работы перед переключением или длительной задачей и гарантирует включение задачи, ожидающей более 15 минут, в следующий совместимый снимок. Фактическая длительность успешного выполнения возвращается в безопасную скользящую оценку.
- Резидентные модели выгружаются по истечении настраиваемого idle keep-alive (по умолчанию сразу), если в очереди нет работы для этой модели. Заблокированный зависимостью шаг workflow для резидентной модели всё ещё удерживает её; работа для другой модели — нет.
- Перед холодным переключением модели брокер выгружает все другие раннеры из управляемого каталога. Неизвестные внешние процессы Ollama не затрагиваются.
- Эксперименты отслеживаются независимо для каждого профиля задачи и модели. Новый кандидат запускается для первых десяти завершённых логических задач каждого применимого профиля, затем эксперимент приостанавливается для проверки владельцем; до этого report gate обратная связь отклоняется. Единственное раннее исключение — `continue_experiment`, которым можно сбросить circuit breaker, открытый двумя последовательными техническими ошибками. Техническая, структурная или контекстная ошибка запускает established fallback. Фрагменты перевода используют один workflow ID и учитываются как одна попытка после итоговой проверки, сохраняя исходную категорию ошибки кандидата при broker fallback.
- Телеметрия экспериментов хранится в течение тридцати дней (`TelemetryRetentionDays`) и содержит только идентификаторы рабочих процессов/задач/моделей, счетчики, результаты, временные метки и оценки использования токенов. Она сообщает о локальном вводе и выводе, общем объеме локальной обработки, избежании генерации в облаке и чистом уменьшении контекста в облаке отдельно. Запросы, ответы, содержимое файлов, байты изображений, пути и секретные данные исключены.

LocalLm предоставляет эти инструменты управления моделями и перевода MCP:

| Инструмент | Назначение |
|---|---|
| `translate_local` | Перевод текста, проверка защищенной структуры Markdown и добавление фактической атрибуции модели. |
| `local_models_status` | Показать установленные, резидентные, отсутствующие рекомендуемые и состояние эксперимента. |
| `local_model_preflight` | Загрузить одну модель с заданным контекстом без содержимого задачи и вернуть подтверждение полного размещения в VRAM. |
| `local_models_sync` | В очереди установка рекомендованных моделей из списка разрешенных. |
| `local_model_experiment_report` | Показывает попытки выполнения логических задач, ошибки, откаты, время выполнения, количество «холодных» и «тёплых» запусков, локальную обработку, избежание генерации в облаке и чистое уменьшение контекста. |
| `local_model_feedback` | Продвигать, продолжать, ограничивать до резервного или отключить одну пару задачи/модель. |

`local_model_experiment_report` берёт `Attempts`, `Successes` и `Errors` из состояния
эксперимента — это те же счётчики, что показывает `local_models_status` и по которым router
решает, когда эксперимент ставить на паузу. Тайминги и токены можно измерить только по
сохранившейся per-task телеметрии, а она ограничена неделей, поэтому `ObservedTasks` говорит,
на скольких записях они держатся. Одно число вместо двух и приводило к тому, что пара с шестью
завершёнными попытками отвечала `Attempts: 1`. Чтение отчёта больше не удаляет телеметрию:
недельная граница применяется записью новой задачи.

`translate_local` — валидируемый путь локального перевода. Вызывающий агент решает, выполнять перевод локально или в облаке; LocalAi не навязывает эту политику.

`read_image` принимает `VisualAnalysis`, `Ocr` или `ImageTranslation`; `ask_local`
принимает явный текстовый профиль задачи. Явная модель является переопределением, подлежащим тем же требованиям по функциональности, установке, контексту и проверке полной VRAM.

Строка notice каждого инструмента сообщает экономию предложением, а не числом, которое надо
истолковать. Задача, слишком мелкая, чтобы что-то сэкономить, так и говорит: скриншот 336×52
стоит около двух десятков токенов при прямом просмотре, и любой полезный ответ о нём длиннее,
поэтому делегирование здесь — честная работа без экономии. Это подсказка о том, что стоит
делегировать, а не сломавшийся счётчик.

## Время выполнения и безопасность

- CodeSearch и LocalLm отправляют всю работу с моделями через устойчивый LocalAi FIFO брокер.
  Прямые конечные точки агента Ollama не поддерживаются.
- Неизменяемое базовое генерирование строится из локального `dev` при наличии, иначе из локального
  `main`. Выбранный основной реф остается стабильным в манифесте репозитория.
- Каждый рабочий каталог получает точный генерация/дерево/недостаточно чистый контент оверлей под
  `%LOCALAPPDATA%\LocalAi`, поэтому ветвь хранит только ее отличия от базы.
- CodeSearch канонизирует индексируемый текст до Windows CRLF в памяти для хеширования, обрезки,
  и вложения. Исходные файлы никогда не переписываются.
- Существующие вектора используются только тогда, когда модель, размерности, формат блока, формат индекса
  и контракт нормализации все совпадают.
- Семантический индекс всегда использует модель вложения, записанную в заголовок индекса.
  Точный лексический поиск остается детерминированным и никогда не вызывает модель чата.
- Общие хуки после фиксации, после слияния, после переписи и после перехода вызывают
  `localai sync`; установка всегда явная.
- Разрешенный командный файл `native` по-прежнему направляется через брокер.
- Проекты MCP зарезервируют стандартный вывод для сообщений протокола stdio.
- Сгенерированные индексы, журналы, файлы процессов, учетные данные и состояние выполнения остаются вне
  Git.
- `RuntimeAcl.Ensure` терпим к путям брокера, которые атомарно перемещаются во время обхода,
  при этом все еще распространяя сбои ACL для путей, которые продолжают существовать.

## Проекты

| Проект | Назначение |
|---|---|
| `src/LocalAi.Contracts` | Контракты обмена: брокер, индекс, репозиторий. |
| `src/LocalAi.Broker` | Устойчивая общесистемная FIFO-очередь и единственный транспорт к Ollama. |
| `src/LocalAi.Broker.Core` | Сама очередь, бутстрап прав на каталог рантайма и всё, что клиент делит с процессом брокера. |
| `src/LocalAi.Broker.Client` | Интеграция клиента и процесса брокера. |
| `src/LocalAi.Launcher` | Стабильный запуск инструментов и атомарная активация неизменяемых версий. |
| `src/LocalAi.Repository` | Идентификация репозитория, манифест и состояние рабочего дерева. |
| `src/LocalAi.Cli` | Синхронизация репозиториев, совместимый транспорт и установка хуков. |
| `src/CodeSearch.Core` | Нарезка на чанки, эмбеддинги, хранилище индекса, overlay и гибридный поиск. |
| `src/CodeSearch.Cli` | Команды индексации, overlay, состояния, сканирования и поиска. |
| `src/CodeSearch.Mcp` | Stdio MCP-сервер с операциями CodeSearch. |
| `src/LocalLm.Core` | Помощники локальных задач, метаданные изображений и оценка токенов. |
| `src/LocalLm.Mcp` | Stdio MCP-сервер с операциями LocalLm. |
| `src/LocalAi.Installer` | GUI-установщик: проверка системы, зависимости, пакет, модели, резидентность, регистрация клиентов. |
| `src/LocalAi.Installer.Core` | То, что установщик делает, а не показывает: проверка подписи, аренда каталога установки, зависимости, блоки инструкций для агентов. |
| `src/LocalAi.ReleaseSigner` | Сборка, подпись и публикация релиза, а также проверка опубликованного манифеста. |
| `tests/*` | Юнит-тесты и интеграционные тесты всех компонентов инструментария. |

## Правила разработки

- Сохраняйте существующие пространства имен, команды, пути выполнения и версии пакетов, если отдельное изменение явно одобряет обновление.
- Отправляйте работу Ollama только через совместный брокер.
- Добавляйте сфокусированные тесты рядом с изменениями поведения и запускайте базовый вариант решения в целом перед публикацией.
- Тест не должен проверять, насколько быстра машина. Ждите наступления условия, а не истечения срока; берите в качестве дочернего процесса небольшую программу под задачу, а не интерпретатор с непредсказуемым временем запуска; поиск зависаний оставьте таймауту прогона, который CI задаёт для всего решения. Срок, в который может не уложиться корректный прогон, ничего не доказывает и выглядит в отчёте как поломка проверяемого кода.
- Ветку под pull request отводите от `main` и держите в ней одну тему. Две темы в одной ветке нельзя ни отревьюить по отдельности, ни откатить по отдельности. Стекать один pull request на другой хуже, чем сделать ребейз: при удалении базовой ветки на мерже GitHub закрывает такой pull request, а не переносит его на `main` — так случилось с #91, и его пришлось пересоздавать. Если два изменения трогают один файл, влейте первое и сделайте ребейз второго.
- Каждый документ в репозитории существует на английском и на русском — как `name.md` и
  `name.ru.md`, — и каждый в первых строках ссылается на свою пару. Документ на одном
  языке считается незаконченным, включая планы и проектные записки.
- Сохраняйте UTF-8 без BOM и окончания строк Windows CRLF для документации репозитория.

Перевод выполнен локальными моделями: qwen2.5-coder:14b и translategemma:12b.

## Участие в разработке

Как изменения собираются, проверяются и ревьюятся — в [CONTRIBUTING.ru.md](CONTRIBUTING.ru.md);
что продукт обещает и как приватно сообщить об уязвимости — в [SECURITY.ru.md](SECURITY.ru.md).

## Лицензия

MIT, текст — в файле [LICENSE](LICENSE).
